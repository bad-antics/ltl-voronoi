// examples/demo.ltl — runnable demo for ltl_voronoi

import std::io
use ltl_voronoi as lib

fn main() {
    io::println("ltl_voronoi demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}