// ltl-voronoi/main.ltl
//
// Fortune's sweep-line Voronoi diagram.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-voronoi v0.1.0")
    io::println("Fortune's sweep-line Voronoi diagram.")
    0
}
